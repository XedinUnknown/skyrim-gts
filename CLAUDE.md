# Claude Instructions

Read and follow `AGENTS.md` for this project's canonical instructions.

Project skills are maintained canonically under `.agents/skills/`. `.claude` is a symlink to `.agents` so Claude Code sees the same `skills/` tree without a separate mirror refresh step.
